spring:
  profiles: development
  jpa:
    show_sql: false
    hibernate:
      ddl-auto: none
  datasource:
    platform: h2
    schema: classpath:db/h2/schema-h2.sql
    data: classpath:db/h2/data-h2.sql
server:
  port: 1111